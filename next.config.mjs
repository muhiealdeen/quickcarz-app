// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.imagin.studio',
      // 'carapi.app',
    ],
  },
};

export default nextConfig;
